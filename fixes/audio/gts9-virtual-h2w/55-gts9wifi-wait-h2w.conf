[Service]
# Gate PA on the virtual jack existing (hard-fail, no '-' prefix).
ExecStartPre=/bin/sh -c 'c=0; until grep -q gts9-virtual-h2w /proc/bus/input/devices; do [ $c -ge 60 ] && exit 1; sleep 1; c=$((c+1)); done'
