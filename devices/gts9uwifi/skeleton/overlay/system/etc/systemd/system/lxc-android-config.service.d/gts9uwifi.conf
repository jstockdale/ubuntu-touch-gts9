# gts9uwifi: use the device wrapper (filtered-bootconfig charger->normal bind
# + stale property-area clean). See /usr/lib/gts9uwifi/start-android-container.
[Service]
ExecStart=
ExecStart=/usr/lib/gts9uwifi/start-android-container
