# sm5714 fuel gauge + charger: without these, capacity reads 0 and UPower
# executes a battery-critical poweroff ~2-3 min after boot.
sm5714_fuelgauge
sm5714-charger
# WiFi: cfg80211 lives in the rootfs modules tree (not vendor_dlkm's
# modules.load, so the audio-bringup walker never touches it) and kiwi_v2
# does not autoload reliably on a virgin flash. Previously persisted only
# on-device as /etc/modules-load.d/gts9u-wifi.conf.
cfg80211
qca_cld3_kiwi_v2
